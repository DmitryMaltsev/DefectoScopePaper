2019.09.02: Обновление программы до версии 1.0.1:
• (f) Проверка состояния границ осуществлялась при любом количестве прошедших профилей, приводило к спаму сброса и сообщений.
• (+) Функция проверки состояния границ была разделена на две, чтобы понимать какая на какой границе проблема.
• (+) Функция удаления старых файлов теперь возвращает список имен удаленных файлов (привязал к логу).
• (+) Дефект имеет метод .ToString.
• (+) Дефекты прописываются теперь не только в базу данных, но и в лог.
• (f) Проверка флага записи в базу данных перед непосредственной записью не проводилась.

2019.09.05: Обновление программы до версии 1.0.2:
• (f) MS SQL является асинхронной в работе и способна сама занимается блокировкой доступа, мы не нужны (с минимальными обратимыми изменениями).
• (f) Алгоритм авто-границы был изменен: вместо "от краев к центру" будет "от центра к краям" (с обратным изменением), скользящее окно 5% → 1%.
• (+) Был добавлен опциональный механизм автоматического сохранения отчетов по заданному жестко расписанию.